import React from 'react';

const StyleSizeQuantity = ({ styleSizeQuantity }) => {
  console.log(styleSizeQuantity);
  //   const quantity = styleSizeQuantity.map((item, key) => (
  //     <div>
  //       {item.quantity}
  //     </div>
  //   ));

  //   const size = styleSizeQuantity.map((item, key) => (
  //     <div>
  //       {item.size}
  //     </div>
  //   ));

  return (
    <div />
  );
};

export default StyleSizeQuantity;
